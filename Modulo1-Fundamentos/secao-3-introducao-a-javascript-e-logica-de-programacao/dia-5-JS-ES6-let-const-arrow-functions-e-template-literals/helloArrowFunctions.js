/* function hello(nome) {
    return `Olá, ${nome}!`
  }
  let nome = 'Ivan';
  console.log(hello(nome)); */

  /* Transforme a função hello em uma arrow function; */
  
  const hello = (nome) => `Olá, ${nome}!`
  
  let nome = 'Ivan';
  console.log(hello(nome));